# E29 S1 Spike — LadybugDB (lbug) v0.19.0 Build Validation

Validates that the `lbug` crate downloads its prebuilt static lib from GitHub
and can create + query a `.lbdb` directory database using the Kùzu-derived API.

## Exit Criteria

See `sddk/e29-s1-build/proposal.md` for full context and
`sddk/e29-s1-build/design.md` for technical approach.
