//! Navigation Service - Handles code navigation operations

/// Service for navigating code structure
pub struct NavigationService;

impl NavigationService {
    /// Creates a new NavigationService
    pub fn new() -> Self {
        Self
    }
}

impl Default for NavigationService {
    fn default() -> Self {
        Self::new()
    }
}
