# Verification Report

**Change**: oss-coverage-expansion
**Version**: N/A
**Mode**: Strict TDD

---

### Completeness
| Metric | Value |
|--------|-------|
| Tasks total | 27 |
| Tasks complete | 27 |
| Tasks incomplete | 0 |

All checklist items in `sdd/oss-coverage-expansion/tasks.md` are marked
complete.

---

### Build & Tests Execution

**Build**: ✅ Passed
```text
Command: cargo check
Result: exit 0
Notes: passed with pre-existing Rust warnings unrelated to this change.
```

**Tests**: ✅ 3 passed / ❌ 0 failed / ⚠️ 0 skipped
```text
Command: cargo test test_manifest_ --lib
Passed:
- sandbox_core::manifest::tests::test_manifest_parse
- sandbox_core::manifest::tests::test_manifest_expand
- sandbox_core::manifest::tests::test_manifest_expand_with_variants
```

**Coverage**: ➖ Not available

---

### Requested re-verification checks
| Check | Evidence | Result |
|------|----------|--------|
| 1. JS/TS manifests use valid stage names | No `name: install` / `name: typecheck` matches remain; JS uses `build`, `syntax`, `test`; TS uses `build`, `build`, `test` | ✅ Passed |
| 2. `sandbox_orchestrator.rs` starts MCP for Go/Java | `cfg.name.contains("go")` and `cfg.name.contains("java")` are present in the MCP startup gate | ✅ Passed |
| 3. `java_repos.yaml` uses immutable SHA | `pinned_sha: "edf4db28affcc4741c79850a3d95bc3f177b5ff9"` | ✅ Passed |
| 4. `sandbox-orchestrator plan` works on all 4 manifests | `cargo run --bin sandbox-orchestrator -- plan ... --format json` succeeded; 40 scenarios expanded | ✅ Passed |
| 5. `sandbox-orchestrator run` on `go_repos.yaml` verifies MCP start | Read/search scenarios now return `pass`; no `no_result` outcomes remain | ✅ Passed |
| 6. `sandbox-orchestrator run` on `java_repos.yaml` verifies MCP start | Read/search scenarios now return `pass`; no `no_result` outcomes remain | ✅ Passed |

---

### Manual runtime verification
| Check | Command / Evidence | Result |
|------|---------------------|--------|
| Manifest planning | `cargo run --bin sandbox-orchestrator -- plan sandbox/manifests/go_repos.yaml sandbox/manifests/java_repos.yaml sandbox/manifests/js_repos.yaml sandbox/manifests/ts_repos.yaml --format json` | ✅ Passed |
| Planned scenario counts | Go 15, Java 5, JavaScript 10, TypeScript 10, total 40 | ✅ Passed |
| Go manifest run | `cargo run --bin sandbox-orchestrator -- run sandbox/manifests/go_repos.yaml --results-dir sandbox/results/verify-go-repos-final --jsonl` | ✅ Passed |
| Java manifest run | `cargo run --bin sandbox-orchestrator -- run sandbox/manifests/java_repos.yaml --results-dir sandbox/results/verify-java-repos-final --jsonl` | ✅ Passed |

Go run outcomes (`sandbox/results/verify-go-repos-final/run.jsonl`):

```text
pass: 6
expected_fail / capability_missing: 9
no_result: 0
```

Java run outcomes (`sandbox/results/verify-java-repos-final/run.jsonl`):

```text
pass: 2
expected_fail / capability_missing: 3
no_result: 0
```

Observed MCP startup evidence:

- `go_realrepo_read_source_*` and `go_realrepo_search_content_*` now return
  `pass` with Go container image `docker.io/library/golang:1.23-alpine`.
- `java_realrepo_read_source_petclinic_default` and
  `java_realrepo_search_content_petclinic_default` now return `pass` with Java
  container image `docker.io/library/eclipse-temurin:21-alpine`.
- `extract_symbols` still returns `Unknown tool: extract_symbols`, but those
  scenarios are declared `capability_missing`, so current manifest expectations
  are satisfied.

---

### TDD Compliance
| Check | Result | Details |
|-------|--------|---------|
| TDD Evidence reported | ❌ | `sdd/oss-coverage-expansion/apply-progress` has no `TDD Cycle Evidence` table |
| All tasks have tests | ❌ | 0/27 tasks map to change-specific automated tests |
| RED confirmed (tests exist) | ❌ | No change-specific RED evidence was reported |
| GREEN confirmed (tests pass) | ❌ | No change-specific GREEN evidence was reported |
| Triangulation adequate | ➖ | No change-specific tests exist to triangulate |
| Safety Net for modified files | ⚠️ | `apply-progress` lists changed files, but no safety-net evidence was reported |

**TDD Compliance**: 0/6 checks passed

---

### Test Layer Distribution
| Layer | Tests | Files | Tools |
|-------|-------|-------|-------|
| Unit | 0 | 0 | Rust built-in available, not used for this change |
| Integration | 0 | 0 | Available in project, not used for this change |
| E2E | 0 | 0 | Available in project, not used for this change |
| **Total** | **0** | **0** | |

---

### Changed File Coverage
Coverage analysis skipped — no coverage tool detected.

---

### Assertion Quality
**Assertion quality**: ➖ No change-specific test files were created or modified,
so there were no assertions to audit.

---

### Quality Metrics
**Linter**: ⚠️ `cargo clippy --bin sandbox-orchestrator -- -W warnings` reported
project-wide warnings, including warnings in `src/bin/sandbox_orchestrator.rs`
**Type Checker**: ✅ `cargo check` passed

---

### Spec Compliance Matrix

| Requirement | Scenario | Test / Evidence | Result |
|-------------|----------|-----------------|--------|
| go-cobra / Manifest Entry | Manifest is schema-valid | `cargo test test_manifest_ --lib` does not validate this specific manifest entry; manual inspection shows valid stage names | ⚠️ PARTIAL |
| go-cobra / Analysis Compatibility | read_file on Go source succeeds | `sandbox/results/verify-go-repos-final/run.jsonl` → `go_realrepo_read_source_cobra_default = pass` | ⚠️ PARTIAL |
| go-cobra / Analysis Compatibility | search_content finds exported symbol | `sandbox/results/verify-go-repos-final/run.jsonl` → `go_realrepo_search_content_cobra_default = pass` | ⚠️ PARTIAL |
| go-bubbletea / Manifest Entry | Manifest entry validates against schema | `cargo test test_manifest_ --lib` does not validate this specific manifest entry; manual inspection shows valid stage names | ⚠️ PARTIAL |
| go-bubbletea / Analysis Compatibility | read_file on main tea source | `sandbox/results/verify-go-repos-final/run.jsonl` → `go_realrepo_read_source_bubbletea_default = pass` | ⚠️ PARTIAL |
| go-bubbletea / Analysis Compatibility | search_content finds Model interface | `sandbox/results/verify-go-repos-final/run.jsonl` → `go_realrepo_search_content_bubbletea_default = pass` | ⚠️ PARTIAL |
| go-lo / Manifest Entry | Manifest entry validates against schema | `cargo test test_manifest_ --lib` does not validate this specific manifest entry; manual inspection shows valid stage names | ⚠️ PARTIAL |
| go-lo / Generics Compatibility | read_file on generic function file | `sandbox/results/verify-go-repos-final/run.jsonl` → `go_realrepo_read_source_lo_default = pass` | ⚠️ PARTIAL |
| go-lo / Generics Compatibility | search_content finds generic constraint | `sandbox/results/verify-go-repos-final/run.jsonl` → `go_realrepo_search_content_lo_default = pass` | ⚠️ PARTIAL |
| java-petclinic / Repo Snapshot Presence | Pinned SHA is reproducible | `sandbox/manifests/java_repos.yaml` now uses immutable SHA `edf4db28affcc4741c79850a3d95bc3f177b5ff9` | ⚠️ PARTIAL |
| java-petclinic / Analysis Compatibility | read_file on a Java entity class | Runtime now proves MCP starts, but scenario still targets `pom.xml`, not `Owner.java` from the spec | ❌ UNTESTED |
| java-petclinic / Analysis Compatibility | search_content finds Spring annotation | Runtime now proves MCP starts, but scenario still searches `Spring`, not `@Entity` from the spec | ❌ UNTESTED |
| js-express / Manifest Entry | Manifest entry validates against schema | Manual inspection shows `build`, `syntax`, `test`; no invalid stage names remain | ⚠️ PARTIAL |
| ts-zod / Manifest Entry | Manifest entry validates against schema | Manual inspection shows `build`, `build`, `test`; no invalid stage names remain | ⚠️ PARTIAL |

**Compliance summary**: 0 fully compliant / 14 reviewed scenarios

Under Strict TDD, no spec scenario can be marked fully compliant because no
change-specific automated tests were added and the only executed Rust tests are
generic manifest expansion tests. Runtime evidence confirms the requested fixes,
but that is not sufficient for full spec compliance under the active TDD rules.

---

### Correctness (Static — Structural Evidence)
| Requirement | Status | Notes |
|------------|--------|-------|
| JS/TS manifest schema stage alignment | ✅ Implemented | `js_repos.yaml` and `ts_repos.yaml` no longer use `install` or `typecheck` stage names |
| Go/Java MCP startup wiring | ✅ Implemented | `sandbox_orchestrator.rs` now includes both Go and Java in the MCP startup allowlist |
| Java immutable SHA pin | ✅ Implemented | `java_repos.yaml` now pins petclinic to a full commit SHA |
| Four-manifest plan expansion | ✅ Implemented | `sandbox-orchestrator plan` expands all 40 scenarios successfully |
| Go runtime MCP startup | ✅ Implemented | Go read/search scenarios now pass instead of returning `no_result` |
| Java runtime MCP startup | ✅ Implemented | Java read/search scenarios now pass instead of returning `no_result` |
| Java spec/design command parity | ⚠️ Partial | Manifest and runtime use Gradle, while current spec/design still describe Maven |
| Java analysis scenario fidelity | ⚠️ Partial | Runtime works, but manifest scenarios still do not exactly match spec targets (`Owner.java`, `@Entity`) |

---

### Coherence (Design)
| Decision | Followed? | Notes |
|----------|-----------|-------|
| Manifest schema alignment | ✅ Yes | Updated JS/TS stage names now conform to schema enums |
| Go/Java container config and runtime wiring | ✅ Yes | Image mapping plus startup gate now allow Go and Java MCP execution |
| Immutable metadata for reproducibility | ✅ Yes | Java `pinned_sha` is now immutable |
| Java validation strategy from design | ⚠️ Deviated | Design/spec still describe Maven, but implementation remains Gradle |

---

### Issues Found

**CRITICAL** (must fix before archive):
- Strict TDD verification still fails because `sdd/oss-coverage-expansion/
  apply-progress` does not contain the required `TDD Cycle Evidence` table and
  there are no change-specific automated tests proving the spec scenarios.
- The Java spec/design still require Maven validation commands, while the
  implemented manifest and runtime behavior use Gradle.

**WARNING** (should fix):
- `java_repos.yaml` runtime scenarios now prove MCP startup, but they still do
  not match the spec exactly: read targets `pom.xml` instead of `Owner.java`,
  and search uses `Spring` instead of `@Entity`.
- `cargo clippy --bin sandbox-orchestrator -- -W warnings` reports existing
  warnings in changed and surrounding Rust code.

**SUGGESTION** (nice to have):
- Add automated manifest-schema tests for the four OSS manifests.
- Add an orchestrator integration test that asserts Go and Java read/search
  scenarios do not regress to `no_result`.
- Update the Java spec/design to match the chosen Gradle workflow, or switch the
  manifest back to Maven if Maven is the real requirement.

---

### Verdict
FAIL

The requested post-fix checks now pass: JS/TS stage names are valid, Go/Java MCP
startup is wired, Java uses an immutable SHA, all four manifests plan
successfully, and Go/Java orchestrator runs now prove MCP startup at runtime.
However, the overall SDD verification still fails because Strict TDD evidence is
missing and the Java implementation still deviates from the current spec/design.
